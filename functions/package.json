{
  "name": "functions",
  "description": "Firebase Cloud Functions for The Turkish Shop",
  "scripts": {
    "lint": "eslint .",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.9.0",
    "resend": "^1.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.6",
    "eslint": "^8.15.0",
    "typescript": "^4.9.0"
  },
  "private": true
}
